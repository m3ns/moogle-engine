# MOOGLE SEARCH ENGINE

The goal of this search engine is to make things easy for new Dark web users to find what they need. And also to make a litle profit by selling services etc.

## What do we need?

## Frontend:
    Homepage:
       • logo(above search bar)
       • search bar(in middle of the page)
Details: Every time a person does a search a request will happen to the backend on '/api/search' in the request we need a parameter to be send with the request called query parameter. The backend gets the request like this '/api/search?q=example'.
